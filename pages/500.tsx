import { NextPage } from 'next';

import { Typography } from '../elements';

const ServerError: NextPage = () => (
  <Typography as="h1">500 - Server Error</Typography>
);
export default ServerError;
